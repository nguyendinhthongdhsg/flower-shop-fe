import ContentDirectory from './ContentDirectory';

const Content = () => {
    return (
        <main>
            content
            <ContentDirectory />
        </main>
    );
};

export default Content;
