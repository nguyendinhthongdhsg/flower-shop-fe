import { DefaultLayout } from '@/Components/Layouts';

const Page = () => {
    return (
        <main>
            <DefaultLayout>home page</DefaultLayout>
        </main>
    );
};

export default Page;
